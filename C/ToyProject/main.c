#include <stdio.h>

#include "Toy_Intro.h"
//#include "Random.h"

// h -> 해당 헤더의 c -> h 함수 정의 -> 해당 h의 c 에서 소스 추가

/*
	f12 : [함수의 이름 마우스 우클릭 - 정의로 이동] 단축키
	이름을 더블 클릭 - f12입력

	ctrl + '-' : 이전 화면으로 돌아가기

	ctrl + k + d : 자동 서식 맞춤
	ctrl + k + c : 범위 주석 설정
	ctrl + k + u : 범위 주석 해제
*/

int main() {
	IntroMenu();

	/*for (int i = 0; i < 10; i++)
	{
		int randomNumber = ReutrnRandomValue();
		int randomRange = ReturnRandomInRange(20, 40);
		printf("랜덤 숫자 %d : %d\n", i+1, randomRange);
	}*/
}